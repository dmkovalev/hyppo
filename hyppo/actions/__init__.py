"""hyppo action registry — typed operations callable by agents over MCP."""

from hyppo.actions import (  # noqa: F401 — side-effect imports populate ACTION_REGISTRY
    diff,
    version,
    virtual_experiment,
)
